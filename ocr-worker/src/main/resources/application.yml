spring:
  application:
    name: ocr-worker
  rabbitmq:
    host: rabbitmq
    port: 5672
    username: guest
    password: guest

dms:
  rmq:
    exchange: dms.exchange
    queue:
      upload: dms.document.uploaded
    routing:
      upload: dms.document.uploaded