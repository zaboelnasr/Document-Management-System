spring:
  application:
    name: Document-Management-System

  datasource:
    url: jdbc:postgresql://localhost:5432/dmsdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
